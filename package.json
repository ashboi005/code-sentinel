{
  "name": "code-sentinel",
  "private": true,
  "workspaces": [
    "apps/proxy-backend"
  ],
  "scripts": {
    "proxy:dev": "bun run --cwd apps/proxy-backend dev",
    "proxy:test": "bun test apps/proxy-backend"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
